<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>・<%= link_to "Delete", task_path(task), data: { turbo_method: :delete, turbo_confirm: "Do you want to delete #{task.title}?" } %></li>
  <% end %>
</ul>

<p><%= link_to "Add a new task", new_task_path %></p>
